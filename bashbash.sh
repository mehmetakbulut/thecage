#!/bin/bash
#Starts up the main python code block in an interpreter shell.
#This allows operator to manually enter commands using the shell instead of the GUI if s/he chooses to do so.
python -i /home/pi/aerial/main.py
